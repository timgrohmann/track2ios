# Uncomment the next line to define a global platform for your project
platform :ios, '13.0'

target 'TrackSquared' do
    use_frameworks!

    # Pods for TrackSquared
    pod 'Alamofire', '~> 5.0.0-rc.2'

    target 'TrackSquaredTests' do
        inherit! :search_paths
        # Pods for testing
    end

end
